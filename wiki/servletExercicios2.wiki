#summary Primeira lista de exercícios objetivos

= Questões =

 # Quais os parâmetros fornecidos ao método Servlet.service()?
   #. !HttpServletRequest
   #. !HttpServletConfig
   #. !HttpServletResponse
   #. !HttpServletContext
 # Marque métodos da classe !HttpServletResponse
   #. getWriter()
   #. getContentType()
   #. setContentType()
   #. setCharacterEncoding()
 # O método getWriter() retorna um objeto do tipo !PrinterWriter?
   #. sim
   #. não
 #. "text/plain" e "image/jpeg" são MIME types que podem ser empregados pelo método setContentType()?
   #. sim
   #. não
 #. Pode-se fazer uma requisição HTTP empregando o método GET por meio de uma aplicação em Java?
   #. sim
   #. não
 #. Pode-se fazer uma requisição HTTP empregando o método POST por meio de uma aplicação em Java?
   #. sim
   #. não
 #. O contêiner web é responsável pela criação de instâncias de servlets?
   #. sim
   #. não
 #. O que se pode dizer do método Service.init(!ServletConfig)?
   #. Não existe
   #. É chamado quando se inicia o contêiner web
   #. Todos os servlets que já trataram requisições necessariamente receberam esta mensagem
   #. Todo servlet necessariamente implementa tal método
= Respostas = 

 #. 1 e 3
 #. 1, 2, 3 e 4
 #. 2 (o correto é java.io.!PrintWriter)
 #. 1
 #. 1
 #. 1
 #. 1
 #. 3 e 4