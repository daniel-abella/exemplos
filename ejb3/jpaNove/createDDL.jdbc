CREATE TABLE PRODUTOINFO (ID BIGINT NOT NULL, FORNECEDOR VARCHAR(255), DATAVALIDADE DATE, PRIMARY KEY (ID))
CREATE TABLE PRODUTO (ID BIGINT NOT NULL, NOME VARCHAR(255), CODIGO INTEGER, PRODUTOINFO_ID BIGINT, PRIMARY KEY (ID))
ALTER TABLE PRODUTO ADD CONSTRAINT PRDUTOPRDUTOINFOID FOREIGN KEY (PRODUTOINFO_ID) REFERENCES PRODUTOINFO (ID)
CREATE TABLE SEQUENCE (SEQ_NAME VARCHAR(50) NOT NULL, SEQ_COUNT DECIMAL, PRIMARY KEY (SEQ_NAME))
INSERT INTO SEQUENCE(SEQ_NAME, SEQ_COUNT) values ('SEQ_GEN', 1)
